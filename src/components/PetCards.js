import React from 'react';
import '../App.css';
import { Route, Link, Switch } from 'react-router-dom';
import Favorites from './Favorites';

const PetCards = (props) => {
	return (
    <h1>this is PetCards</h1>
    )
};

export default PetCards;
